
# AUTOMATICALLY GENERATED
# DO NOT EDIT THIS FILE DIRECTLY, USE /templates/conf/fluent.conf.erb

@include "#{ENV['FLUENTD_SYSTEMD_CONF'] || 'systemd'}.conf"
@include "#{ENV['FLUENTD_PROMETHEUS_CONF'] || 'prometheus'}.conf"
@include kubernetes.conf
@include conf.d/*.conf

<match **>
   @type datadog
   @id out_datadog

   api_key "#{ENV['DATADOG_API_KEY']}"

   use_json "#{ENV['DATADOG_USE_JSON'] || true}"
   include_tag_key "#{ENV['DATADOG_INCLUDE_TAG_KEY'] || true}"
   tag_key "#{ENV['DATADOG_TAG_KEY'] || 'tag'}"
   dd_source "#{ENV['DATADOG_SOURCE'] || 'fluentd'}"
   dd_tags "#{ENV['DATADOG_TAGS'] || 'fluentd:fluentd'}"
   dd_sourcecategory "#{ENV['DATADOG_SOURCE_CATEGORY'] || 'fluentd'}"
</match>
